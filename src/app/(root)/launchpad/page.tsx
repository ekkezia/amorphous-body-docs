import { PROJECT_URL } from '../../../../shared';
import ImageSlide from '../../_components/image-slide';

export default function LaunchpadPage() {
  return (
    <>
      <ImageSlide numOfImages={217} path={`${PROJECT_URL}/launchpad/amorphous_body_ekezia_launchpad`} />
    </>
  );
}
