import { PROJECT_URL } from '../../../../shared';
import ImageSlide from '../../_components/image-slide';

export default function MidiGraphPage() {
  return (
    <>
      <ImageSlide numOfImages={725} path={`${PROJECT_URL}/midigraph/amorphous_body_midigraph_ekezia`} />
    </>
  );
}
